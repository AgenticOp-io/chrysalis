{
  "name": "@chrysalis/emit-fastify",
  "version": "2.0.1",
  "private": true,
  "description": "WebIR → Fastify + SQLite emitter. Second backend; proves WebIR target portability.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@chrysalis/archaeology": "workspace:*",
    "@chrysalis/emit-shared": "workspace:*",
    "@chrysalis/webir": "workspace:*"
  },
  "devDependencies": {
    "@chrysalis/ingest": "workspace:*"
  }
}
