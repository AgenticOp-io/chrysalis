{
  "name": "@chrysalis/verify",
  "version": "0.0.0",
  "private": true,
  "description": "Replay engine: runs generated handlers against recorded traces, attributes divergences to IR nodes, emits correctness reports.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@chrysalis/webir": "workspace:*",
    "@chrysalis/oracle": "workspace:*"
  }
}
