{
  "name": "@chrysalis/compat",
  "version": "1.0.1",
  "private": true,
  "description": "PHP standard-library compatibility shim for generated TypeScript output. Fallback only; lowering passes should prefer native TS idioms.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  }
}
