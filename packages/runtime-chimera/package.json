{
  "name": "@chrysalis/runtime-chimera",
  "version": "2.0.1",
  "private": true,
  "description": "Dual-stack runtime: traffic router, session bridge, schema lens. Lets PHP and the new stack coexist behind one origin.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@chrysalis/verify": "workspace:*"
  }
}
