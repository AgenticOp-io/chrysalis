/**
 * @chrysalis/ingest hub native origin adapters (Java / Go / Ruby / C# — Phase 41c).
 */
export {
  parseJavaRoutes,
  parseGoRoutes,
  parseRubyRoutes,
  parseCsharpRoutes,
  SCHEMA_VERSION as HUB_NATIVE_BRIDGE_SCHEMA_VERSION,
  type HubNativeRoute,
  type HubNativeParseResult,
} from "@chrysalis/hub-native-bridge";
