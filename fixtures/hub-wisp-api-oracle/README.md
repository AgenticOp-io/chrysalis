# WISP API oracle (Phase 28d pilot)

Minimal Express oracle for **`GET /api/tenants`** trace capture. Not the full `backend-services` stack — deterministic tenant list for verify replay against native CWL handlers in `fixtures/hub-wisp-management/api-proxy.cwl`.

Capture: `pnpm run wisp:api-trace-capture`
