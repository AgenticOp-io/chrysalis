<main class="portal-shell login-page">
  <div class="login-card">
    <h1>{{ $title }}</h1>
    @if($showHint)
      <p class="hint">Portal credentials</p>
    @endif
    <button type="button">Continue</button>
  </div>
</main>
