# hub-flagship-kotlin

Twenty-route **Spring Kotlin `@RestController`** app mirroring `hub-flagship-express` / java / go / csharp (scalar + `mapOf` JSON + path/query + `ResponseEntity` status). No invented product UI (**D6447**).

## Prove

```bash
pnpm run hub:kotlin-flagship
pnpm run hub:complete-conversion-prove:kotlin
```

Expect `stGreen`+`stClosed` (`proveProfile: cwl-api`).
