# hub-flagship-csharp

Twenty-route **ASP.NET Minimal API** app mirroring `hub-flagship-express` / `hub-flagship-python` / `hub-flagship-go` (scalar + JSON + path/query + status). No invented product UI (**D6447**).

## Prove

```bash
pnpm run hub:csharp-flagship
pnpm run hub:complete-conversion-prove:csharp
```

Expect `stGreen`+`stClosed` (`proveProfile: cwl-api`).
