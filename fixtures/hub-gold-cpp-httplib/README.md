# hub-gold-cpp-httplib

Secondary **cpp-httplib** dialect (nlohmann::json + `set_content` / `res.status` / `req.matches` / `get_param_value`). Express-depth 20-route mirror; does **not** replace Crow `hub-flagship-cpp` D6448-ST.

```bash
pnpm run hub:cpp-httplib-smoke
```
