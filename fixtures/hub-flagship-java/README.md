# hub-flagship-java

Twenty-route **Spring `@RestController`** app mirroring `hub-flagship-express` / python / go / csharp (scalar + JSON + path/query + `ResponseEntity` status). No invented product UI (**D6447**).

## Prove

```bash
pnpm run hub:java-flagship
pnpm run hub:complete-conversion-prove:java
```

Expect `stGreen`+`stClosed` (`proveProfile: cwl-api`).
