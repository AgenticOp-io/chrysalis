# hub-cobol-layout-odo

Labeled **public** OCCURS DEPENDING ON (ODO) layout fixtures for inventory peel **G10121**.

| File | Provenance |
| --- | --- |
| `OccursDepending1.cbl` | JRecord `zTest/Cobol/occursDependingOn/OccursDepending1.cbl` |
| `OccursDepending.cbl` | JRecord `zTest/Common/CopyBook/Cobol/OccursDepending.cbl` |

Upstream: [bmTas/JRecord](https://github.com/bmTas/JRecord) (BSD/Apache-family — keep off `main` only as these small labeled extracts).

**Not:** EXTFMAP substitute · CICS runtime · LCB claim · inventing ODO execution.
