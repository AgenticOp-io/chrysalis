# hub-cobol-layout-renames

Labeled **public** Level-66 `RENAMES` fixtures for inventory peel **G10122**.

| File | Provenance |
| --- | --- |
| `renames_r4_redefines.cpy` | EffortlessMetrics/copybook-rs `fixtures/copybooks/renames_r4_redefines.cpy` |
| `renames_r5_odo.cpy` | EffortlessMetrics/copybook-rs `fixtures/copybooks/renames_r5_odo.cpy` |

Upstream: [EffortlessMetrics/copybook-rs](https://github.com/EffortlessMetrics/copybook-rs) (AGPL — labeled extracts only; blobs stay off `main` full clone).

**Not:** EXTFMAP · rename runtime invent · LCB claim.
