# hub-flagship-ruby

Twenty-route **Sinatra** app mirroring `hub-flagship-express` / python / go / csharp (string scalars + `status` + `json` + path/query). No invented product UI (**D6447**).

## Prove

```bash
pnpm run hub:ruby-flagship
pnpm run hub:complete-conversion-prove:ruby
```

Expect `stGreen`+`stClosed` (`proveProfile: cwl-api`).
