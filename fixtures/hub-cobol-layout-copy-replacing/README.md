# hub-cobol-layout-copy-replacing

Labeled **public** `COPY … REPLACING` fixtures for inventory peel **G10124**.

| File | Provenance |
| --- | --- |
| `copy-replacing-leading.cbl` | Pattern from [meyfa/CobolCraft](https://github.com/meyfa/CobolCraft) issue #412 / `DD-ENTITY` LEADING usage (labeled host stub) |
| `copy-replacing-pseudotext.cbl` | Classic IBM-style pseudo-text `==…== BY ==…==` pairs (gnu-honest catalog sample) |
| `dd-inventory-slot-replacing.cpy` | Nested LEADING extract shape from CobolCraft `DD-ENTITY.cpy` (`COPY … REPLACING LEADING`) |

Upstream CobolCraft is MIT. Blobs are thin labeled extracts — not full game corpus on `main`.

**Not:** EXTFMAP · copy-expansion runtime invent · INITIALIZE/INSPECT REPLACING (different verbs) · LCB claim.
