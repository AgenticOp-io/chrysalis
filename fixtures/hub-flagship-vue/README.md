# hub-flagship-vue

Twenty-route **Express API** embedded in a **Vue SFC** `<script>` (honest SPA+API fixture), mirroring `hub-flagship-express`. Uses the existing Vue→JS AST lift (`hub-gold-vue-*`); not invented product UI (**D6447**). Minimal template shell only — API authority is the script routes.

## Prove

```bash
pnpm run hub:vue-flagship
pnpm run hub:complete-conversion-prove:vue
```

Expect `stGreen`+`stClosed` (`proveProfile: cwl-api`).
