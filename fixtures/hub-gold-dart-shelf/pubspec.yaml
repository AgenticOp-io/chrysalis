name: hub_gold_dart_shelf
description: Chrysalis hub-gold Dart Shelf foundation (route corpus only; not run in CI).
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
