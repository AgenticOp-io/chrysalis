# lift-helper-gap-probe

Documents the gap between **D283 structural dedupe** and future **IR helper lifting** (**`docs/IR-HELPER-LIFTING.md`** B1).

- **`chrysalis_scale_alpha`** and **`chrysalis_scale_beta`** — same pure arithmetic, different local names (`$x` vs `$y`); **identical effect signatures**, **different** structural dedupe keys.

Vitest: **`packages/ingest/tests/lift-helper-gap-probe.test.ts`**.
