# CWL scripts — sync note

**Primary holder:** `../chrysalis-cwl/scripts/hub-ingest/` (and the rest of the CWL pillar).

These files are mirrored copies so the convert monorepo / GitHub CI keep resolving.

**Always check `chrysalis-cwl` first.** Edit language tooling there, then sync copies here (or switch to a submodule). Never land divergent CWL script behavior only in convert.