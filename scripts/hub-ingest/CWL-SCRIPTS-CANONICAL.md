# CWL scripts — sync note

Canonical language tooling lives in `../chrysalis-cwl/scripts/hub-ingest/`.
These files are mirrored copies so the convert monorepo / GitHub CI keep resolving.
When editing language tooling, prefer `chrysalis-cwl` then sync copies here (or switch to a submodule).
