/**
 * Generic CWL convert libraries (neutral names).
 *
 * Lifted from the WISP Module_Manager POC harness — POC fixtures stay under
 * `fixtures/hub-wisp-management/`; engine/convert code lives here without `wisp` in the path.
 *
 * Prefer importing from `scripts/lib/*`. Legacy `scripts/wisp-*` paths are temporary shims.
 *
 * See `docs/initiative-knowledge.v1.json` extractMap + `docs/UNIVERSAL-TRANSLATOR-PATH.md`.
 */
export {};
