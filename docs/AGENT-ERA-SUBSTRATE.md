# Agent-era substrate (Chrysalis bets)

> **Status:** active entry (**G10116** / **D6541**)  
> **Not:** Parity Bonds, Inter-org Exchange, FedNow-of-commits, or FDE weld slogans without a cross-lane charter.

## Recommendation (locked)

| Priority | Bet | Why Chrysalis |
| --- | --- | --- |
| **P0 medium** | **Hole Type System** + **CWL-Above-Code** | Attacks how software is authored; already brand law (**D6442**/**D6447**) + CWL hub |
| **P1 packaging** | **Dispose Plane** (entry) | Packages existing propose≠dispose into a gate other agents must pass |
| **Deferred** | World-from-Traffic / Continuity Runtime / Parity Bonds | Need FDE cross-lane + commercial; do not invent inside this monorepo |

Dual primary (**D6540**) stays: COBOL ∥ language deepen fill **ingress**. These bets define **ownership after lift**.

**CWL-Above-Code thesis:** CWL is the source of record; TypeScript / Hono / Fastify (and other emit targets) are **disposable** backends — greenfield prove is CWL → WebIR → gold verify, not “pick a forever language.”

## Gates

```bash
pnpm run hub:hole-type-system-smoke
pnpm run hub:cwl-above-code-smoke
pnpm run hub:dispose-plane-smoke
pnpm run hub:agent-era-substrate-smoke   # composite
```

**Deepen (G10117–G10119 pack):** taxonomy includes `runtime:` / `exec:` and types dialect honesty catalogs (`drogon` / `revel` / `nancy` / `deno-serve` / `tapir`) via `classifyTaxonomyCatalogFixtures`. Dispose Plane issues an agent-facing dispose certificate (`chrysalis.dispose.certificate` in `scripts/lib/dispose-certificate.mjs`) — merge only when certificate.ok + confirm. Convert apply writes `.chrysalis/hub-convert.dispose-certificate.json` and refuses apply without it.

## Explicit non-goals

- Do not invent a world model inside Chrysalis (FDE owns BYOW).
- Do not force-settle holes to claim Hole Type System “complete.”
- Do not replace origin compare for migration prove (**D6443** until cutover).
