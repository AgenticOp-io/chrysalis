node_modules/
dist/
build/
*.tsbuildinfo
.DS_Store
.env
.env.local
# Local editor state; shared team defaults live in .vscode/settings.json (tracked).
.vscode/*
!.vscode/settings.json

# Firebase CLI local cache (never commit)
.firebase/

# Cursor editor local state; shared rules live under .cursor/rules/
.cursor/*
!.cursor/rules/
!.cursor/rules/chrysalis.mdc
coverage/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Chrysalis-specific
.chrysalis/
# Local GCP service account keys (never commit; see scripts/create-gcp-vm-service-account.ps1)
.chrysalis-gcp-sa-key.json
*-gcp-sa-key.json
# Go CLI shim local build artifacts
go/shim/chrysalis-go
go/shim/chrysalis-go.exe
# Local SQLite for tiny-blog (seed / oracle; DDL is schema.sql)
fixtures/tiny-blog/blog.sqlite
traces/
reports/
.holes/
tmp/
tmp-emit-out/

# Local release tarballs (see pnpm run release:artifacts, docs/RELEASE.md)
release/

# Local scratch emits (not CI fixtures)
generated/tmp-*/

# Emitted / reproducible tree (see CONTRIBUTING: proof-of-concept and pilot trees)
# Tracked: generated/tiny-blog sources (reference). Not tracked: lockfiles, DBs, extra emit targets.
generated/**/package-lock.json
generated/**/*.sqlite
generated/**/*.sqlite-journal
# Rebuilt in CI via scripts/emit-tiny-blog-fastify.mjs
generated/tiny-blog-fastify/
generated/flagship-laravel-min/
generated/flagship-laravel-min-fastify/
generated/flagship-laravel-full/
generated/flagship-laravel-full-fastify/
# Rebuilt locally / in CI from fixtures/tiny-n1
generated/tiny-n1/

# Composer Laravel scaffold output (see flagship/laravel-full/README.md)
flagship/chrysalis-laravel-work/

# parser-bridge nikic / composer (lock file is committed; vendor is not)
packages/parser-bridge/vendor/
packages/parser-bridge/composer.phar
packages/parser-bridge/composer-setup.php
